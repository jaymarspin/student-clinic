import { StocksGetPipe } from './stocks-get.pipe';

describe('StocksGetPipe', () => {
  it('create an instance', () => {
    const pipe = new StocksGetPipe();
    expect(pipe).toBeTruthy();
  });
});
